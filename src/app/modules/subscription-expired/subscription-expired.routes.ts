import { Routes } from '@angular/router';
import { SubscriptionExpiredComponent } from './subscription-expired.component';

export default [
    { path: '', component: SubscriptionExpiredComponent },
] as Routes;
